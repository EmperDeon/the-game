package render.gui.widgets.std;

public class StdImage extends StdWidget{

  public StdImage(String text, int x, int y, int w, int h){
  super(text,text,text);
  setXY(x,y);
  setHeight(h);
  setWidth(w);
 }
}
